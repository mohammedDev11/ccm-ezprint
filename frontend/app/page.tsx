import { MainNavbar } from "./sections/mainPage/components/MainNavbar";

const page = () => {
  return (
    <main className="w-full overflow-x-clip">
      <MainNavbar />
    </main>
  );
};

export default page;
